# ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

alias pbpaste='xsel --clipboard --output'

function rmvim() {
	for pat in {'*~','*.swp','*.swo'}
	do
		find . -name "${pat}" -exec rm {} \;
	done
}
